dist
node_modules
.lighthouseci
coverage
# Generated by scripts/pipeline/generate-lighthouse-config.mjs and verified
# against that generator's exact output by CI ("Verify lighthouse configs are
# up to date"). Prettier would collapse the short URL arrays to one line and
# fight the generator on every commit, so the generated dir is left as-is.
lighthouse/
static/admin/config.yml
# Vendored, minified CMS build (self-hosted Cumbre Labs fork) — not our source.
static/admin/sveltia-cms.js
