User-agent: *
Allow: /
Disallow: /admin/

# Sitemap URL must match the canonical domain advertised by `<link rel="canonical">`
# on every page (siteConfig.url) — otherwise crawlers may treat the sitemap as
# unauthorized for the canonical host.
Sitemap: https://www.viajesapu.com/sitemap.xml

# LLM-friendly site overview (https://llmstxt.org/) — a curated, plain-text map
# for AI crawlers/assistants, generated dynamically by
# src/routes/llms.txt/+server.ts. Served at the canonical host below.
# LLMs: https://www.viajesapu.com/llms.txt
