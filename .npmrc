# Refuse to install/run when the active Node version doesn't satisfy
# `engines.node` in package.json. Without this, npm would just warn and let
# you install on the wrong Node, silently breaking pre-commit + build.
engine-strict=true
