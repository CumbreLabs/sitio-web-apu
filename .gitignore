# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.svelte-kit
*.local

# Local env / secrets — never commit OAuth keys, tokens, analytics IDs, etc.
.env
.env.*
!.env.example

# Playwright MCP scratch output
.playwright-mcp/

# Local design-source working files — brand kit, posters, PDFs, raw photo
# dumps (250+ MB of originals). These do NOT belong in the web repo; the site
# serves optimized derivatives from static/media + static/srcset instead.
# (srcset itself stays tracked.)
/assets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.lighthouseci/
coverage/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
docs/
